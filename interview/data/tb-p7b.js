// 第七篇 · 第 19-20 章 —— 教材正文
window.TB_SECTIONS = window.TB_SECTIONS || {};
Object.assign(window.TB_SECTIONS, {
  "19.1": {
    id: "19.1",
    t: "记忆管理",
    from: "《智能体设计模式》第 8 章 · Memory Management",
    why: "不学这节，你会把「它怎么又忘了」当成模型不行，而不是当成一个该设计的地方：无状态智能体永远只能做一次性问答。",
    learn: "能画出短期记忆与长期记忆两条存放路径，并说出状态写入为什么必须走事件追加、而不是直接改状态字典。",
    body: `### 一句话说清
智能体的记忆分两层：短期记忆活在上下文窗口里，是一次对话的草稿纸。长期记忆存在外部存储里，是跨会话的档案柜。

### 展开讲
症状很好认：同一个用户第二次来，它又问了一遍预算；五步任务做到第三步，前两步的结论已经没了。

短期记忆就是上下文窗口，装最近的消息、工具返回和中间结论。窗口有硬顶，满了只能截断或摘要压缩，而压缩必然丢信息，这是要提前设计的取舍。

长期记忆放在外部存储，用时先检索再灌回短期上下文。三个设计点：写入要留痕，状态更新必须走事件追加，别直接改状态字典：书里说得明白，直接改不会进事件历史、可能不被持久化、有并发问题、时间戳也不更新。范围要分清，用户级、应用级、单轮有效这三类数据的生命周期完全不同。长期记忆还能再分三类，记事实的、记经历的、记规则的。

**不重复讲的部分**：短期与长期记忆的机制、框架里对应的组件在第 8.6 节已经讲透，本节只讲模式视角：什么时候该补哪一层记忆，以及写入为什么必须走事件流。

### 动手看
~~~python
class Memory:
    def __init__(self, max_turns=8):
        self.short = []            # 短期：上下文窗口里的那一页
        self.max_turns = max_turns

    def add(self, role, text):
        self.short.append({"role": role, "text": text})
        if len(self.short) > self.max_turns:
            self.short = self.compress(self.short)   # 摘要压缩，必然丢信息

    def compress(self, msgs):
        return msgs[-2:]           # 真实实现交给模型做摘要

    def remember(self, key, value):
        store.put(("user", user_id), key, value)      # 长期：外部存储

    def recall(self, query):
        return store.search(("user", user_id), query=query)[:3]

def build_prompt(mem, question):
    return {"facts": mem.recall(question),
            "recent": mem.short,
            "question": question}
~~~

### 什么时候不该用
一次性问答和无状态批处理，加记忆只多出维护成本和隐私负担。上下文还装得下时，先截断加摘要就够了。最常见的误用是绕过事件流直接改状态字典：当场看着生效，重启后全没了。`,
    keypoints: [
      "短期记忆就是上下文窗口，有硬顶，满了只能截断或摘要压缩，压缩必然丢信息，这是要提前设计的取舍。",
      "长期记忆放在外部存储，用时先检索再灌回短期上下文，检索不到等于没有。",
      "状态必须通过事件追加写入；直接改状态字典不会进事件历史、可能不被持久化、有并发问题、时间戳不更新。",
      "记忆要按范围分层（用户级 / 应用级 / 单轮有效），不同范围的持久性和可见性完全不同。",
      "长期记忆可分语义（事实）、情景（经历）、程序（规则）三类，记住的地址不同，检索方式也不同。",
    ],
    pitfalls: [
      "把「模型忘了」一律归因于模型能力，忽略了记忆该在架构里显式设计。",
      "用长上下文窗口替代长期记忆：窗口再长也是临时的，会话一结束就没了，而且每轮都要重算一遍。",
      "直接操作状态字典图省事，测试时看不出问题，上线重启后数据丢失且难以定位。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "02-AI-Agent开发/记忆与上下文工程.md", "01-名词与概念/上下文窗口.md"],
    qs: ["dp-08"],
    ev: [],
  },

  "19.2": {
    id: "19.2",
    t: "学习与适应",
    from: "《智能体设计模式》第 9 章 · Learning and Adaptation",
    why: "不学这节，你会在两个极端之间摇摆：要么以为智能体上线后会自己变好，要么一上来就想去微调模型：两条路都贵，都容易翻车。",
    learn: "能对一项要让智能体「越用越好」的需求，判断该改提示词、改知识还是改权重，并说出为什么没有评测集就不能自动改提示词。",
    body: `### 一句话说清
学习和适应是让智能体根据新经验和数据，改变自己的思考方式、行动方式或知识。类比学徒：不是靠换脑子变强的，是先记下哪次做成了、哪次砸了，下次照着改手法。

### 展开讲
症状是能力固定：环境变了、用户口味变了，它原地不动，只能靠人手工改提示词。书里列的学习途径有好几条：强化学习、监督学习、无监督学习、基于 LLM 的少样本与零样本、在线学习、基于记忆的学习。对应用开发者，真正会用到的是后三条。

先分清两件事：学习是内部变化，适应是这种变化在外的可见表现。没有可见表现的「学习」，你没法验证。

再分清四条改法的代价梯度。改提示词最快、可回滚、不用训练；改知识把成功做法和踩过的坑写进知识库，下次检索回来。改策略是换工具、换路由、换流程。改权重就是微调。微调书里讲了两种做法：PPO 走两步，先拿人类偏好数据训一个奖励模型，再用它微调，模型可能找漏洞把奖励刷上去。DPO 跳过奖励模型，直接用偏好数据提高被偏好回答的概率、压低不被偏好回答的概率，更简单也更稳。但微调成本高、迭代慢、样本要成规模，多数团队不该从这起步。

书里两个案例值得记住。SICA 会翻自己历史版本的档案，按成功率、耗时、算力加权打分挑出最好的一版，直接改自己的代码库，再跑基准测试把结果记回档案，循环迭代。架构里有专职子智能体和一并发运行的监督者，负责发现打转或停滞并可以中止执行，还必须跑在容器里做隔离。AlphaEvolve 用 Flash 生成大量候选、Pro 精炼，加自动评估和进化框架去发现算法，书里给了它在数据中心调度、芯片设计和矩阵乘法上的具体战绩。OpenEvolve 能进化整个代码文件而不只是单个函数。

**和相邻章节的分界**：反思模式（第 18.4 节）是单次任务内的自我批判，用完就散。本节讲跨任务、要留痕、能累积的改进：每一次改进都得先有评测证明它真的更好。

### 动手看
~~~python
archive = []   # 每次尝试留一条档：做法 / 结果 / 代价

def record(task, strategy, score, cost):
    archive.append({"task": task, "strategy": strategy,
                    "score": score, "cost": cost})

def best_strategy(task):
    rows = [r for r in archive if r["task"] == task]          # 同一类任务
    rows.sort(key=lambda r: (-r["score"], r["cost"]))          # 先看效果，再看代价
    return rows[0]["strategy"] if rows else None

def improve(task, candidate_prompt, eval_set):
    before = evaluate(current_prompt, eval_set)                # 没有评测就不改
    after = evaluate(candidate_prompt, eval_set)
    if after > before:
        promote(candidate_prompt)      # 评测通过才回灌到提示词或知识库
    else:
        discard(candidate_prompt)
    record(task, candidate_prompt, after, cost_estimate())
~~~

### 什么时候不该用
没有评测集，就别让智能体自动改提示词，改坏了你发现不了，只会一路漂移。任务分布稳定、样本又少时，固定提示词比「会学习」的系统更可靠。想靠微调解决「答得不够好」，先确认不是提示词和知识层的问题，否则你花钱买的是一个更贵的漂移。`,
    keypoints: [
      "学习是内部变化，适应是外部可见的变化；没有可见表现的学习无法验证。",
      "改进的代价梯度是：改提示词 < 改知识 < 改策略 < 改权重（微调），多数团队应该从最便宜的一端起步。",
      "PPO 走「先训奖励模型再微调」两步，可能被模型刷分；DPO 跳过奖励模型直接用偏好数据，更简单也更稳。",
      "SICA 靠「翻历史版本档案 → 按加权分挑最优 → 改自己的代码 → 重跑基准 → 记回档案」循环自我改进，并用独立监督者防止打转。",
      "AlphaEvolve 是「LLM 生成候选 + 自动评估 + 进化选择」的组合，能发现人和模型都没想到的算法。",
    ],
    pitfalls: [
      "把微调当第一选择：成本高、迭代慢、样本要成规模，且要改的东西提示词和知识层往往也能改。",
      "没有评测集就自动回灌提示词，结果是静默漂移：短期看不出，长期不可复现。",
      "自我改进的系统不做隔离和监控：会改自己代码的智能体必须能执行命令，不给容器隔离和外部监督就是在裸奔。",
      "只记录成功案例，不记录失败和代价，档案里挑出来的「最优」会偏向耗时最长的那一条。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "02-AI-Agent开发/Agent自进化.md"],
    qs: ["dp-09"],
    ev: [],
  },

  "19.3": {
    id: "19.3",
    t: "模型上下文协议（MCP）",
    from: "《智能体设计模式》第 10 章 · Model Context Protocol (MCP)",
    why: "不学这节，你会为一个已经有标准答案的问题写定制集成：每换一个模型、每接一个系统，就重写一遍工具胶水。",
    learn: "能说出 MCP 与函数调用在标准化、架构、发现方式上的三条差别，并判断一个固定小工具集的项目该不该上 MCP。",
    body: `### 一句话说清
MCP 是一套开放标准，让任何模型都能接任何外部系统。类比书里那个：函数调用是给 AI 一套定制扳手，MCP 是装一套标准插座：它不提供工具，但合规工具插上能用。

### 展开讲
症状是 N 乘 M 的集成爆炸：每个模型乘每个系统都要写一遍胶水。MCP 用客户端-服务器架构把乘积压成加法。

它跟函数调用的差别有三条。标准化：函数调用各家专有，MCP 是开放标准。架构：前者是模型与应用工具逻辑的一对一交互，后者是客户端连服务器的结构。发现：前者要把工具写进上下文，后者能运行时问服务器会什么。

最容易踩的坑书里专门讲了：MCP 只是接口契约，底层设计不好照样没用。票务系统若只能一条条取完整工单，让它总结所有高优先级工单，量大时又慢又不准。该先给接口加过滤和排序。同理，把 PDF 包成 MCP 服务是白干，该先做成模型能读的文本。

**别处已经讲过的**：MCP 的协议机制、服务器与客户端写法在第 8.5 节已经讲透，本节只讲模式视角：什么时候该引入 MCP、什么时候函数调用就够了。

### 动手看
~~~python
from fastmcp import FastMCP

mcp_server = FastMCP()

@mcp_server.tool
def greet(name: str) -> str:
    """生成个性化问候。
    Args:
        name: 要问候的人名。
    Returns:
        问候语字符串。
    """
    return "Hello, " + name + "! Nice to meet you."

if __name__ == "__main__":
    mcp_server.run(transport="http", host="127.0.0.1", port=8000)

# 客户端侧：模型应用连上去，并可以只放行部分工具
root_agent = LlmAgent(
    name="fastmcp_greeter_agent",
    instruction="用 greet 工具问候对方。",
    tools=[MCPToolset(
        connection_params=HttpServerParameters(url="http://localhost:8000"),
        tool_filter=["greet"],       # 只暴露需要的工具，减少误选
    )],
)
~~~

### 什么时候不该用
固定几个函数、只用一个供应商的简单应用，直接函数调用更省事。最典型的反模式是把遗留接口原样包一层当成 MCP 服务交差：连接通了，但数据格式对模型不友好、缺过滤排序，等于把复杂度推给模型。`,
    keypoints: [
      "MCP 解决的是 N 乘 M 的集成爆炸，用一套开放标准把乘法变成加法。",
      "与函数调用的三条差别：标准化（专有 vs 开放）、架构（一对一 vs 客户端-服务器）、发现（写进上下文 vs 运行时动态查询）。",
      "服务器公开三类东西：资源是静态数据，工具是可执行函数，提示是交互模板。",
      "本地走标准输入输出上的 JSON-RPC，远程走可流式 HTTP 与服务器发送事件。",
      "MCP 只是接口契约，底层 API 不友好照样没用：该先加上过滤、排序这类确定性能力，该返回模型能读的文本而不是 PDF。",
    ],
    pitfalls: [
      "把 MCP 当万能胶：以为接上就万事大吉，忽略了底层数据格式和接口设计才是决定成败的地方。",
      "给模型暴露几十个工具不做过滤，直接拉高误选率：书里的示例都用了工具白名单。",
      "对固定小工具集的简单项目也强行上 MCP，多出一层协议和维护负担却没有换来任何复用。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "01-名词与概念/MCP模型上下文协议.md", "02-AI-Agent开发/MCP协议实战.md"],
    qs: ["dp-10"],
    ev: [],
  },

  "19.4": {
    id: "19.4",
    t: "目标设定与监控",
    from: "《智能体设计模式》第 11 章 · Goal Setting and Monitoring",
    why: "不学这节，你会把「帮我写得好一点」当成目标交给智能体，然后发现它自己判自己全对：写验收标准的功夫早晚要补，早补省事。",
    learn: "能把一句模糊需求改写成可判定的目标（谁判、判什么、判据是什么、不达标怎么办），并说出裁判和被裁判是同一个模型时的风险。",
    body: `### 一句话说清
目标设定是告诉智能体做成什么样算做完，监控是让它边做边对照目标检查进度。类比规划旅行：先定目的地，再看清出发点，算上交通和预算的约束，才排得出订票、打包、赶车这一串步骤。

### 展开讲
症状是没有方向。它只会被动回应，做完一步不知道下一步该不该走，也说不清自己成了没有。目标设定解决「往哪走」，监控解决「走到了吗」。

核心功夫是把模糊需求翻译成可判定的目标。判定标准要能回答四个问题：谁判、判什么、判据是什么、不达标怎么办。书里提倡对齐 SMART：具体、可衡量、可实现、相关、有时限。举个对照：「代码要简洁易读」不可判定；换成「函数不超过 30 行、不出现递归、带 3 个边界用例断言」，就能机器判，也能被别人复核。

书里示例是这么写的：给一个用例加一份目标清单，让模型生成代码，再让模型评审给反馈，然后单独问一句「目标达成了吗」，只准回答 True 或 False：要单词回答是为了方便停机。判 False 就带上反馈重写，循环有轮数上限，判 True 才存档。

书里也把坑写明了：模型可能没真正理解目标却判自己成功；同一个模型既写代码又判质量，更难发现自己走错方向。示例里的监控很基础，进程有一路转下去的风险。模型不会魔法般产出没问题的代码，你仍然要跑要测。更稳的做法是把角色拆开，写代码的、审代码的、写文档的、写测试的各司其职，审查者独立出来判断会客观得多。在框架里，目标通常写在指令里，监控落在状态管理和工具交互上。

**和教材其它节的分工**：规划模式（第 18.6 节）负责生成步骤序列，本节负责定义什么算做完、谁来判。前者管路径，后者管验收。

### 动手看
~~~python
GOALS = [
    "函数不超过 30 行",                 # 可数
    "不出现递归",                       # 可查
    "含 3 个边界用例断言",              # 可查
]

def judge(code: str, goals: list) -> bool:
    # 只允许回答一个词，方便停机判断
    reply = llm.invoke(REVIEW_PROMPT.format(code=code, goals=goals)).strip().lower()
    return reply == "true"

def run(use_case, max_rounds=5):
    code, feedback = "", ""
    for i in range(max_rounds):                  # 必须有上限，否则会一直转
        code = llm.invoke(build_prompt(use_case, GOALS, code, feedback))
        if judge(code, GOALS):
            return save(code, use_case)
        feedback = llm.invoke(feedback_prompt(code, GOALS))   # 带上反馈再改
    return save(code, use_case)                  # 到上限就交，别假装达标
~~~

### 什么时候不该用
单步任务不需要目标监控循环，每多一层就多一份延迟、token 和死循环风险。让同一个模型既当选手又当裁判时要格外小心。也别为了「可判定」改掉目标的真实含义：如果真目标是用户满意，换成点击率达标只是换了个好量的指标。`,
    keypoints: [
      "目标设定管「往哪走」，监控管「走到了吗」，两者合起来才是反馈回路，缺一个都不能自主收口。",
      "可判定目标要能回答谁判、判什么、判据是什么、不达标怎么办，并尽量对齐 SMART。",
      "让裁判只回答 True 或 False（或极短离散选项），是为了让流程能干净地停机。",
      "让同一个模型既生成又评审，它更难发现自己走错方向；把审查角色拆出去会明显更客观。",
      "循环必须有轮数上限，到上限就如实交付，不要默认它会自己达标。",
    ],
    pitfalls: [
      "把「简洁易读」「用户喜欢」这类词直接当目标，结果每次评审结论都不一样，流程不可复现。",
      "以为模型判 True 就等于代码没问题：生成的东西仍然要跑、要测。",
      "监控循环不设轮数上限，任务卡住时成本无限增长。",
      "为了让指标好看而悄悄改掉目标的真实含义，指标达标了，问题还在。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "02-AI-Agent开发/Agent核心架构.md", "02-AI-Agent开发/Agent评估与调试.md"],
    qs: ["dp-11"],
    ev: [],
  },

  "20.1": {
    id: "20.1",
    t: "异常处理与恢复",
    from: "《智能体设计模式》第 12 章 · Exception Handling and Recovery",
    why: "不学这节，你的智能体会在第一次工具超时就整个挂掉，或者更糟：无限重试把额度和下游服务一起拖下水。",
    learn: "能对五类典型失败各说出一条对应的恢复路径，并说明为什么参数错误和业务拒绝不能靠重试解决。",
    body: `### 一句话说清
提前想清楚智能体出错了怎么办。类比开车：不是只练不撞车，是同时备好刹车、备胎和拖车。

### 展开讲
症状是脆：工具返回格式不对就崩、下游 500 就崩、模型吐出没法解析的东西就崩。书里把模式拆成三段。检测看四样：工具输出是否无效或格式错、接口有没有 404 或 500 错误码、响应时间是否异常长、输出格式是否偏离。处理手段有五种：记日志、重试、走备选路径、优雅降级只留部分功能、通知人或别的智能体。恢复包括回滚、诊断、自我纠正或重规划、升级给上一级。

好用的写法是先给失败分类，再给每类配一条恢复路径，而不是都塞进同一个 try：

瞬态失败（网络抖动、限流、503）：重试，但必须带指数退避、加随机抖动、设次数上限，三个参数缺一个都可能变成重试风暴把下游压死。

参数错误（400、入参不合法、格式错）：重试同样的请求一百次也不会成功，该把错误原文回灌给模型改参数，或者直接拦下。

依赖不可用（服务宕机、区域故障）：换路，退回备用工具或数据源，或降级成只做还能做的那部分。

业务性失败（余额不足、市场关闭、无权限）：这不是技术故障，重试只是重复失败。该记录、回滚、上报，把决定权交回业务侧。

结果不可验证（输出没法校验、自检不通过）：带更严格约束重跑一次，还不行就升级给人，别硬凑答案。

书里示例是分层兜底：三个智能体串行，第一个取精确位置，第二个看状态里有没有主路失败的标记、有就换范围级接口顶上，第三个统一读结果对用户说话。

常见误用：无限重试；错误一把抓不分类；异常吞掉继续跑让数据静默损坏。用异常处理掩盖设计问题。日志只有堆栈没有上下文。

**这一节在全书里的位置**：语言级的异常处理在 Python 报错速查那一节讲 try 与 except；本节讲智能体级的：失败不是抛一次就结束，而要分类、选恢复路径、在状态里留痕。书里也提到它常与反思模式（第 18.4 节）配合：失败后先分析原因，再带改进做法重试。

### 动手看
~~~python
import time, random

def call_tool(tool, args, max_retries=3):
    for attempt in range(max_retries):
        try:
            return tool(**args)
        except TransientError as e:            # 瞬态：退避 + 抖动 + 上限
            if attempt == max_retries - 1:
                return fallback_tool(**args)   # 换路
            time.sleep((2 ** attempt) + random.random())
        except BadRequestError as e:           # 参数错：重试无用，回灌让模型改
            return {"need_fix": str(e), "try_again_with": repair(args, e)}
        except BusinessError as e:             # 业务拒绝：回滚 + 上报
            rollback(args)
            notify_human("business_rule", str(e))
            return {"status": "escalated"}
    return {"status": "degraded"}              # 兜底：降级而不是崩掉
~~~

### 什么时候不该用
确定性的校验不该交给模型兜底，参数不合法、字段缺失在代码层拦住更便宜可靠。对幂等性不清楚的写操作（付款、下单、发消息）不要盲目重试，一次重试就是一笔重复交易。某个工具天天超时，该修的是那个工具，而不是把重试次数从三加到十。`,
    keypoints: [
      "三段式：检测（错误码、格式、超时、输出异常）→ 处理（日志、重试、备选、降级、通知）→ 恢复（回滚、诊断、自我纠正、升级）。",
      "失败要分类配路径：瞬态重试、参数错回灌修正、依赖不可用换路或降级、业务失败回滚上报、结果不可验证带约束重跑或升级。",
      "重试必须同时有指数退避、随机抖动和次数上限，缺一个都可能把下游压垮。",
      "重试不能解决所有问题：400 类参数错误和业务性拒绝，重试只会重复失败。",
      "兜底出口要唯一：主路失败自动走备路，最后统一由同一个出口对用户说话。",
    ],
    pitfalls: [
      "只加 try 不分类，把所有错误一把抓，结果该换路的在重试、该上报的被吞掉。",
      "静默吞掉异常继续跑，表面跑通、数据已经在悄悄变坏。",
      "对幂等性不明的写操作重试，制造出重复扣款、重复下单。",
      "日志只记堆栈不记上下文（哪个工具、哪组参数、哪一轮），线上出事后没法复盘。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "02-AI-Agent开发/Agent评估与调试.md", "04-Python/新手报错速查.md"],
    qs: ["dp-12"],
    ev: [],
  },

  "20.2": {
    id: "20.2",
    t: "人在回路",
    from: "《智能体设计模式》第 13 章 · Human-in-the-Loop",
    why: "不学这节，你要么把不可逆的动作交给没人看的自动链路，要么反过来什么都弹窗确认，把审核的人训练成闭眼点通过。",
    learn: "能用高代价、不可逆、合规敏感这三条判定一个动作该不该设人工闸门，并说出事事都确认会带来什么后果。",
    body: `### 一句话说清
把人的判断力和 AI 的吞吐量接在一起，在关键动作执行前留一道人的闸门。类比开飞机：大部分航程能自动飞，起飞降落还是得有人签字。

### 展开讲
症状是全自动的边界放错了：模型在需要细致判断、伦理推理、模糊语境的场合本来就不可靠，而越是这样的事越贵、越不好回头。

判定标准是本节重点：命中高代价（错了损失大到不能承受）、不可逆（发了、删了、付了、写进生产库就回不去）、合规敏感（法律、医疗、金融、隐私）任何一条，就该在动作前设闸门。反过来，高频、低风险、可回滚的批量操作不该拦。

书里的案例基本都对得上这三条：边界内容升级人工审核员；极端天气下自动驾驶交还控制权。高风险可疑交易由分析师下结论。情绪激烈的客诉转人工。书里也点出为什么有些领域必须如此：大额贷款终审要看管理层品格，量刑涉及复杂的道德推理，都不是模型能替代的。

书里还给了个变体叫人在循环外：人定策略，AI 执行。人定下「七成科技股三成债券、单家公司不超过 5%、跌过 10% 自动卖出」，AI 实时盯盘成交。呼叫中心经理定下路由规则，AI 当场执行。人管慢策略，机器管快动作。

两个方向的误用都要说清。该拦的不拦：把不可逆动作放进无人确认的链路，只在提示词里写「请谨慎」当护栏：这类软提醒真出事时挡不住东西。不该拦的硬拦：事事都要人确认，会把人训练成看也不看就点通过，闸门变成形式。人的吞吐量也有限，书里把缺乏可扩展性列为首要缺点——现实方案通常是混合的：规模给自动化，准确性给人工。

几个硬要求：闸门设在执行之前，事后审批不叫人在回路；要给审核者足够上下文，只丢一句话让他点通过等于没审。升级要有触发条件与超时兜底。隐私数据给人看前要脱敏。

**不重复讲的部分**：本节讲什么时候必须把人拉进来、闸门设在哪里。怎么找出系统里本来就该拦的地方，是 Agent 评估与调试那一节的事。反思（第 18.4 节）是让模型挑错，人在回路是让有权限的人挑错，不能互相替代。

### 动手看
~~~python
RISKY = {"send_email", "delete_file", "pay", "publish", "update_production_db"}

def needs_human(action: dict, policy: dict) -> bool:
    if action["name"] in RISKY:                 # 不可逆 / 高代价
        return True
    if policy["min_amount"] and action.get("amount", 0) >= policy["min_amount"]:
        return True                             # 金额越线
    if action.get("touches") in policy["regulated"]:
        return True                             # 合规敏感领域
    return False

def run_action(action, policy, timeout_s=600):
    if not needs_human(action, policy):
        return execute(action)
    ticket = open_review(action, context=enough_context(action))  # 给足背景
    decision = wait_for_human(ticket, timeout=timeout_s)          # 超时要有兜底
    if decision is None:
        return {"status": "timeout", "action": "hold&notify"}     # 不静默放行
    return execute(action) if decision["approved"] else {"status": "rejected"}
~~~

### 什么时候不该用
高频、低风险、可回滚的操作不要设人工闸门，那是拿人的吞吐量换不存在的安全。最该警惕的是形式化的人在回路：审核者拿不到足够上下文、也没有改动权力，这道闸门只增加延迟。`,
    keypoints: [
      "判定标准就三条：高代价、不可逆、合规敏感，命中任一条就该在动作执行前设闸门。",
      "闸门必须在执行之前；事后审批只是追认，不是人在回路。",
      "人在循环外这个变体同样重要：人定慢策略，AI 执行快动作，规则本身还是人写的。",
      "事事都确认会把人训练成闭眼点通过，等于没有把关，还丢掉了吞吐量。",
      "这个模式的首要缺点是缺乏可扩展性，现实方案是自动化扛规模、人工保准确。",
    ],
    pitfalls: [
      "把「请谨慎操作」这类提示词当成护栏，而不是设真正的拦截逻辑。",
      "让审核者只看一句话就点通过，把闸门做成形式，出事时反而更没人负责。",
      "只设升级入口不设超时兜底，任务挂在等人状态里静默停止。",
      "敏感数据原样推给人看，没做脱敏，把安全问题从模型搬到了工位上。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "02-AI-Agent开发/Agent评估与调试.md", "03-AI辅助开发/VibeCoding方法论.md"],
    qs: ["dp-13"],
    ev: [],
  },

  "20.3": {
    id: "20.3",
    t: "知识检索（RAG）",
    from: "《智能体设计模式》第 14 章 · Knowledge Retrieval (RAG)",
    why: "不学这节，你会在两件事上误判：以为模型「应该知道」公司内部的规定，以及以为上了向量库就等于解决了胡说。",
    learn: "能说清检索加增强两步各自在干什么，并判断一份资料该直接放进上下文还是该建检索库。",
    body: `### 一句话说清
回答之前先去外部知识库查一遍，把查到的片段拼进提示词让模型开卷答题。类比考试：闭卷容易记混，开卷能翻书，答案还指得到页码。

### 展开讲
症状是知识墙：模型的内部知识停在训练截止那一刻，公司制度和私有文档它都不知道，硬答就是一本正经地编。RAG 拆两步：检索，在知识库里按语义找出最相关的片段；增强，把片段加进提示再交给模型生成。关键是语义搜索，措辞和原文不一样也能命中。

要记住的边界：答案散在多处时检索可能凑不齐；分块差会引入噪声带偏模型。知识库要预先处理并定期同步，还推高延迟、成本和提示词长度。两个演进方向各有价格：图 RAG 用知识图谱存实体关系，能跨文档综合，但构建维护贵、延迟高。Agentic RAG 在检索上加一层推理，会校验来源新旧、协调矛盾、拆子查询、缺口时调外部搜索，代价是更复杂更慢更贵，这层推理本身也会成为新的错误源。

**和相邻章节的分界**：分块、嵌入、向量检索的机制在第 7.2 节已经讲透，本节只讲模式视角：什么时候值得上检索，什么时候直接把资料放进上下文更划算。

### 动手看
~~~python
def build_index(docs, chunk_size=500, overlap=50):
    chunks = []
    for d in docs:
        for i in range(0, len(d), chunk_size - overlap):
            chunks.append(d[i:i + chunk_size])      # 切块要留住上下文
    return vector_store.add(texts=chunks, embeddings=embed(chunks))

def rag_answer(question, k=5):
    hits = vector_store.search(embed([question])[0], top_k=k)
    context = "\\n\\n".join(h.text for h in hits)     # 增强：拼进提示
    prompt = ANSWER_TMPL.format(context=context, question=question)
    return llm.invoke(prompt)                       # 顺带返回来源，便于引用
~~~

### 什么时候不该用
知识量小到能直接塞进上下文时别上向量库，多一层检索就多一份延迟和噪声。知识变化极快时同步跟不上，不如直接调实时接口。需要跨多份文档推理的问题，普通向量检索凑不齐证据，要么上图 RAG，要么先加一层校验。`,
    keypoints: [
      "RAG 就两步：按语义检索相关片段，再把片段增强进提示词，把闭卷变成开卷。",
      "能用的关键是语义搜索而不是关键词匹配，配套的是嵌入、相似度、分块和向量库。",
      "向量检索之外还有 BM25 这类关键词检索，混合搜索往往比单用一种更稳。",
      "RAG 也会失效：证据散在多处、分块质量差引入噪声、来源矛盾、同步不及时、延迟与成本上升。",
      "进阶方向各有权衡：图 RAG 擅长跨文档综合但维护贵，Agentic RAG 会校验与补缺但更慢更贵、自身也会犯错。",
    ],
    pitfalls: [
      "以为接了向量库就不会胡说：检索进来的噪声同样能让答案跑偏，而引用还会给人虚假的确定感。",
      "分块切得太碎或太大：太碎丢上下文，太大把无关内容一起带进提示。",
      "知识库只建不更新，答案引用一份早就过期的制度。",
      "把需要跨文档推理的问题丢给普通向量检索，然后归因于模型能力不行。",
    ],
    rel: ["12-智能体设计模式/00-模式总览·21个模式速查.md", "01-名词与概念/RAG检索增强生成.md", "00-小白课堂/小白课-RAG开卷考试.md"],
    qs: ["dp-14"],
    ev: [],
  },
});
