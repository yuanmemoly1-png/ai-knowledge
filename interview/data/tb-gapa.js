// 第一篇 地基篇 · 缺口增补 GAP-A：2.5 Linux 与命令行 · 2.6 测试与质量保障 · 3.9 Docker 与容器化 —— 教材正文
window.TB_SECTIONS = window.TB_SECTIONS || {};
Object.assign(window.TB_SECTIONS, {
  "2.5": {
    id: "2.5",
    t: "Linux 与命令行",
    why: "部署上线要去的服务器没有桌面，只有命令行；不会它，你连服务器都进不去，看日志、查进程更无从下手。",
    learn: "能说出六个场景各自的命令组是干什么的；能把 grep、awk、sort、uniq、head 用管道串成「统计日志里最多的错误」一条龙，并逐段解释每一段在做什么。",
    body: `### 一句话说清
命令行就是「用打字代替点鼠标」操作电脑：敲一行、回车、电脑照办。部署应用要去的那台服务器没有桌面可点，命令行是它唯一的遥控器。

### 展开讲
为什么必须会。写代码的电脑有鼠标有窗口，但部署上线要去的那台 24 小时开机的服务器为省资源不装桌面，连上去只有一黑屏加一个闪烁光标，从此一切操作都是打字：部署、看日志、查进程、杀掉卡死的程序全靠命令行。不会它，「部署」两个字无从谈起。

命令按场景记，别当字典背。六个场景覆盖九成日常：走路：cd 进目录、ls 列内容、pwd 看自己在哪、mkdir 建目录；看文件——cat 一次倒全、less 翻页、head 看开头、tail -f 盯住日志往下滚。找东西：find 按文件名找、grep 按内容找。权限——chmod 改文件权限、sudo 借管理员身份执行。进程：ps aux 列出在跑的程序、top 看谁最吃资源、kill 按编号结束某个进程。网络——ping 测通不通、curl 模拟一次 HTTP 请求、ssh 连远程服务器。

真正值钱的不是单个命令，是**管道**：竖线 | 把左边命令的输出直接喂给右边当输入，一条流水线就架起来了。这就是 Unix 哲学：小工具各干一件事并把它干好，串起来能解大问题。配套的还有重定向：> 把输出写进文件（覆盖旧的），>> 追加到文件末尾。

**别处已经讲过的**：Git（2.1）只是命令行里的一个程序，那一节讲怎么存档。这一节讲它脚下这层 shell 本身：应用搬上服务器之后，连机器、看日志、查进程靠的全是这里。

### 动手看
先造几行假日志，再统计哪类错误出现最多：排障时最常用的一条流水线：
~~~bash
cd /tmp
echo "2026-09-17 10:21 INFO 服务启动" > app.log
echo "2026-09-17 10:22 ERROR 数据库连接超时" >> app.log
echo "2026-09-17 10:23 ERROR 数据库连接超时" >> app.log
echo "2026-09-17 10:24 ERROR 磁盘写入失败" >> app.log

grep ERROR app.log | awk '{print $4}' | sort | uniq -c | sort -rn | head
~~~
逐段拆开读：
~~~text
grep ERROR app.log   只留下含 ERROR 的行
awk '{print $4}'     按空格把每行切成列，取第 4 列（错误描述）
sort | uniq -c       相同的排到一起，各数一次出现几回
sort -rn             按次数从多到少排
head                 只取前几行
~~~
跑出来排最上面的「2 数据库连接超时」就是今天最该修的错。没有 Linux 电脑也能练：Windows 装 WSL，或者用 ssh 连上租来的云服务器，在真环境里敲。

### 什么时候不该用
两条纪律。第一，不背全部参数：记动词就够，参数用到再查：--help 是每条命令自带的说明书。第二，rm -rf 是全部命令里最危险的一条：-r 连文件夹一起删，-f 不再询问直接删，组合起来能把整台服务器删成空机。敲它之前先 ls 看清要删的是什么；路径里带变量的 rm -rf，先把变量 echo 出来看清楚再执行。本地整理文件时鼠标拖得快就用鼠标——命令行是必需品，不是耍酷。`,
    keypoints: [
      "服务器没有桌面，命令行是唯一入口：部署、连机器、看日志、查进程全靠打字，不会它谈不上部署。",
      "命令按场景记：走路 cd/ls/pwd/mkdir，看文件 cat/less/head/tail -f，找东西 find/grep，权限 chmod/sudo，进程 ps/top/kill，网络 curl/ssh/ping。",
      "管道 | 把左边命令的输出喂给右边当输入，重定向 > 覆盖写、>> 追加写；小工具各干一件事串起来用，是命令行真正的价值。",
      "参数不用背，--help 是每条命令自带的说明书，用到再查。",
      "rm -rf 不问直接删且没有回收站：执行前先 ls 确认路径，路径带变量时先 echo 展开看看。",
    ],
    pitfalls: [
      "rm -rf 一条命令删光整个目录甚至整台服务器且不可恢复，是新手最重的翻车，没有之一。",
      "uniq -c 之前忘了先 sort，数出来全是 1：uniq 只合并相邻的重复行。",
      "连上服务器不看 pwd 就动手，命令跑在错误的目录里，改错删错文件。",
      "tail -f 是盯住模式、不会自己退出，按 Ctrl 加 C 收工，别误以为终端死了。",
    ],
    rel: [
      "04-Python/环境搭建与包管理.md",
      "07-工具与资源/常用工具清单.md",
      "00-小白课堂/小白课-部署上线.md",
    ],
    qs: ["ops-01"],
    ev: [],
  },

  "2.6": {
    id: "2.6",
    t: "测试与质量保障",
    why: "AI 生成的代码看起来对不等于跑起来对；没有测试，改一处坏一处，越改越怕，最后整个项目不敢动。",
    learn: "能用 TestClient 给 FastAPI 接口写出带断言的测试并用 pytest 跑绿；能说出对 LLM 输出断言结构而不是断言精确内容，并写出一个只验结构（字段齐全、长度区间、关键词命中）的测试。",
    body: `### 一句话说清
测试就是一小段替你自动验货的程序：每次改完代码，它把关键功能跑一遍、和预期答案核对，不一致就红脸报警。像给代码配的自动对卷机。

### 展开讲
为什么现在必须学，因为你的代码越来越不是你亲手写的。AI 生成的代码**看起来对 ≠ 跑起来对**：语法完整、命名讲究、注释像模像样，逻辑却照样能错一格，光靠读分不出「对」和「像对」。人学习要靠做题戳破「看会的错觉」，代码也一样：没有自动测试的项目改一处坏一处，坏在哪不知道，只能一遍遍手动点回去验证，越改越怕，最后整个项目不敢动。

pytest 记三样就够。断言：assert 后跟一个条件，不成立就报失败；运行：敲 pytest，自动收集 test_ 开头的文件和函数、挨个执行。fixture：给每个测试铺一份干净的初始数据，测完自动清场，免得上一个测试留下的数据污染下一个。测 FastAPI 接口不用真把服务跑起来，用 TestClient 模拟请求就行：有个替身客人在代码里帮你点菜。

核心分野在测 LLM 应用。普通代码输出确定，1 加 1 永远是 2，可以断言精确相等；LLM 的输出每次都在变，你断言它逐字等于某句话，测试就时绿时红、形同虚设。正确姿势是只验「结构」：返回是不是合法 JSON、字段全不全、长度在不在合理区间、该命中的关键词有没有出现。措辞不许较真，格式必须较真。

**和教材其它节的分工**：Agent 的离线评测（攒几十个用例、跑通过率、看轨迹质量）在 8.9。这一节讲写进代码、每次改动自动运行的自动化测试：一个管「模型行不行」，一个管「代码对不对」。

### 动手看
先测确定性输出：FastAPI 小店的接口，装好 pytest 和 httpx 后新建 test_shop.py：
~~~python
# 文件名 test_ 开头，pytest 才收得到
from fastapi.testclient import TestClient
from main import app            # 之前开的那间 FastAPI 小店

client = TestClient(app)

def test_add():
    r = client.get("/add", params={"a": 1, "b": 2})
    assert r.status_code == 200        # 断言一：请求成功
    assert r.json()["result"] == 3     # 断言二：算得对（确定输出，敢精确比）

def test_hello():
    r = client.get("/hello/小明")
    assert "小明" in r.json()["message"]   # 命中即可，不逐字比对
~~~
~~~bash
pip install pytest httpx
pytest -v
~~~
两行绿字就是通过。再练 LLM 的部分：一个把模型输出解析成 JSON 的函数，重点测「正常输入」和「模型胡说」两条路都走得通。
~~~python
import json

def parse_args(text):            # 放进 main.py
    try:
        return json.loads(text)
    except ValueError:
        return None              # 解析失败必须兜底

def test_parse_ok():
    assert parse_args('{"dish": "宫保鸡丁"}') == {"dish": "宫保鸡丁"}

def test_parse_garbage():
    assert parse_args("我不会 JSON") is None
~~~
真实调模型的接口同理，只验结构不验原文：断言返回是字典、summary 字段在、长度不超上限：三次运行措辞不同，照样稳定通过。

### 什么时候不该用
原型期别追覆盖率。功能没定形时快速试错最要紧，等功能稳定、要反复改了再回头补关键路径的测试。反过来，对不确定的输出别用精确相等断言：它必坏，坏的不是代码，是断言写法。最后，自动化测试替代不了真机点一点，LLM 应用整体的行不行要靠跑评测集，那是另一层的事。`,
    keypoints: [
      "AI 生成的代码看起来对不等于跑起来对：语法、命名、注释都像样，逻辑照样能错，读代码分不出「对」和「像对」。",
      "pytest 三件套：assert 下断言、pytest 命令收集并执行 test_ 开头的文件和函数、fixture 给每个测试铺干净初始数据。",
      "测 FastAPI 接口用 TestClient 加 httpx，不用真把服务跑起来就能模拟请求、读返回。",
      "测 LLM 应用的分界线：输出不确定，断言结构（JSON 合法、字段齐全、长度区间、关键词命中），不断言精确内容。",
      "测试的价值是让改动敢下手：改坏了当场红脸，而不是三天后由用户替你发现。",
    ],
    pitfalls: [
      "对 LLM 输出断言逐字相等，测试时绿时红，最后大家习惯性忽略红脸，测试形同虚设。",
      "测试之间共享脏数据互相污染，一个测试留下的记录把下一个测试搞挂：用 fixture 每次铺干净数据。",
      "原型期死磕覆盖率数字，为简单函数写没有断言价值的测试，把做功能的时间耗光。",
      "测试全绿就当万事大吉：界面还得人真机点一遍，LLM 应用还得跑评测集。",
    ],
    rel: [
      "02-AI-Agent开发/Agent评估与调试.md",
      "04-Python/零基础防卡指南.md",
      "00-小白课堂/学习自律与检验方法.md",
    ],
    qs: ["ops-02"],
    ev: [],
  },

  "3.9": {
    id: "3.9",
    t: "Docker 与容器化",
    why: "「在我机器上是好的」是最常见的事故：环境一换项目就跑不起来；不打包环境，作品也没法让别人一条命令复现。",
    learn: "能写出一份让 FastAPI 项目构建成功的 Dockerfile 并说清每条指令的用途；能跑通 build、run -p、ps、logs、exec 一整轮，并解释为什么依赖要放在代码前面 COPY。",
    body: `### 一句话说清
Docker 是打包术：把代码、Python 版本、全部依赖连同运行环境打成一个镜像，照镜像跑起来的容器到哪台机器都一个味道。镜像=菜谱，容器=照菜谱做出来的一桌菜。

### 展开讲
为什么必须打包。同一个项目，你电脑上跑得好好的，搬上服务器就报错：Python 版本不一样、某个库没装、系统缺个文件：「在我机器上是好的」是工程师之间的百年老梗。托管平台替你把环境包了，但只在它的平台上有效；一旦要自己管服务器，或者想让任何人（比如看你作品集的面试官）敲一条命令就把你的项目原样跑起来，靠的就是容器。一次构建，到处运行。

Dockerfile 是菜谱正文，五条指令撑起九成场景。FROM 选底镜像（在哪个现成系统上装 Python）；WORKDIR 定厨房（容器里的工作目录）；COPY 把文件从你电脑搬进镜像。RUN 在构建阶段执行命令，装依赖靠它。CMD 写明容器开桌时执行什么。

分层与缓存是效率关键。每条指令的结果是一层，下次构建时没变的层直接复用、不重做，所以顺序有讲究：先 COPY 依赖清单、RUN 装依赖，最后才 COPY 业务代码：你天天改代码但依赖不动，把最贵的装依赖层放前面，改一行代码只需重做最便宜的一层。

**这一节在全书里的位置**：部署上线（3.7）讲「把店搬到哪里开」：托管平台拎包入住、云服务器买地盖房。这一节讲「搬走之前怎么打包」——Docker 打的包，搬到哪台机器都一个味道。

### 动手看
装好 Docker Desktop（Linux 上装 Docker 引擎），项目根目录放两个文件：requirements.txt 写两行 fastapi 和 uvicorn；Dockerfile 如下，main.py 用之前那间 FastAPI 小店的：
~~~dockerfile
FROM python:3.12-slim          # 底镜像：官方 Python 的精简版
WORKDIR /app                   # 容器里的项目目录
COPY requirements.txt .        # 先只拷依赖清单
RUN pip install --no-cache-dir -r requirements.txt   # 装依赖，最贵的一层
COPY . .                       # 再拷全部代码，改代码不连累上面几层
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
~~~
构建加开桌，五条命令走一轮：
~~~bash
docker build -t my-shop .            # 照菜谱做菜，. 表示菜谱在当前目录
docker run -d -p 8000:8000 my-shop   # 开一桌：-d 后台跑，-p 左宿主机端口右容器端口
docker ps                            # 看哪些桌开着，记下容器名
docker logs 容器名                    # 看这桌的后厨日志，报错都在这
docker exec -it 容器名 bash           # 走进容器里逛逛，exit 退出
~~~
浏览器打开 localhost:8000，和本地一模一样：但这回它跑在一个自带全套环境的盒子里，这盒子搬到任何装了 Docker 的机器上都能再开一桌。

### 什么时候不该用
本地跑个脚本不需要容器：python xxx.py 一秒的事，为它写 Dockerfile 是杀鸡用牛刀。第一个上线的作品也先别碰，托管平台照旧最省事，容器是要自己管服务器、或想让别人一键复现时才请出场的工具。最后一条：K8s 是管几百个容器的调度系统，先把 docker run 玩明白：单机没摸熟就上集群，学到的只有挫败感。`,
    keypoints: [
      "容器解决「在我机器上是好的」：代码加 Python 版本加依赖加系统库打成一个镜像，一次构建，到处运行。",
      "镜像和容器是菜谱和菜的关系：镜像是只读模板，容器是照它跑起来的实例，同一份镜像能开无数桌。",
      "Dockerfile 五指令：FROM 选底镜像、WORKDIR 定目录、COPY 搬文件、RUN 构建时装依赖、CMD 定启动命令。",
      "分层缓存的顺序原则：先拷依赖清单装依赖、后拷代码，改代码才不用重装依赖。",
      "常用五命令：build、run -d -p、ps、logs、exec；-p 左边是宿主机端口，右边是容器端口。",
    ],
    pitfalls: [
      "依赖和代码一起 COPY，每改一行代码依赖层全部重做，构建慢到怀疑人生：顺序写错是新手第一坑。",
      "容器里的服务绑在 127.0.0.1（uvicorn 不加参数的默认值），-p 映射了浏览器照样打不开，容器里必须监听 0.0.0.0。",
      "以为容器能存数据：容器一删数据全没，数据库这类要留的文件要挂载到容器外面。",
      "单机容器还没玩明白就上 K8s，把时间耗在集群配置上，容器本身的概念反而没建立。",
    ],
    rel: [
      "00-小白课堂/小白课-部署上线.md",
      "01-名词与概念/部署与上线.md",
      "09-全栈开发/全栈项目实战.md",
    ],
    qs: ["ops-03"],
    ev: [],
  },
});
